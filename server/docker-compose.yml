version: "3.8"

services:
  server:
    build:
      context: ../
      dockerfile: Dockerfile.server
    ports:
      - "2567:2567"
    environment:
      # Optional: Discord webhook notified when a visitor starts the game
      - DISCORD_WEBHOOK_URL=${DISCORD_WEBHOOK_URL:-}
